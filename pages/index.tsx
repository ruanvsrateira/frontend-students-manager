import React from 'react';

import { AppBar, Button, Container, makeStyles, Theme, Toolbar, Typography, useTheme } from '@mui/material';

import Header from '../src/components/Header';


const Home: React.FC = () => {
  return(
    <Header />
  );
}

export default Home;
